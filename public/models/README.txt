Place your GLB/GLTF assets here. Example path to use in code:

useGLTF('/models/your-model.glb')

Note: The app expects '/models/fox.glb' by default. Replace with your own file or rename your GLB to fox.glb.